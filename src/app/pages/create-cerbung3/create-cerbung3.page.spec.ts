import { ComponentFixture, TestBed } from '@angular/core/testing';
import { CreateCerbung3Page } from './create-cerbung3.page';

describe('CreateCerbung3Page', () => {
  let component: CreateCerbung3Page;
  let fixture: ComponentFixture<CreateCerbung3Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(CreateCerbung3Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
